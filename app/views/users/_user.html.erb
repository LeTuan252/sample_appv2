<li>
  <%= gravatar_for user, size: Settings.users.size_user %>
  <%= link_to user.name %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t(".del"), user, method: :delete,
      data: {confirm: t(".you_sure")} %>
  <% end %>
</li>
